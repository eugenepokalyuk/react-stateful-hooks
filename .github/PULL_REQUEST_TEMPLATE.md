<!-- Thanks for contributing! Keep PRs focused — one logical change per PR. -->

## What & why

<!-- What does this change and what problem does it solve? Link any related issue. -->

Closes #

## Checklist

- [ ] `npm run lint` passes
- [ ] `npm run typecheck` passes
- [ ] `npm test` passes (added/updated tests, including the SSR path)
- [ ] `npm run build` succeeds
- [ ] Updated the README and the "Hooks at a glance" table (if user-facing)
- [ ] Added an entry under `## [Unreleased]` in CHANGELOG.md
